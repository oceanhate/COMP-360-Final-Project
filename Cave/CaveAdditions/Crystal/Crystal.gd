extends Node3D


# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass
	#var random_color : Color = Color(randf_range(0.0, 1.0), randf_range(0.0, 1.0), randf_range(0.0, 1.0), 0.39)
	#$Crystal2/Crystal.mesh.surface_get_material(0).albedo_color = random_color
	#$Crystal2/Crystal.mesh.surface_get_material(0).emission = random_color

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
